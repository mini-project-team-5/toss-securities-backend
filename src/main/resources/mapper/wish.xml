<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tossclone.securities.dao.WishDao">

	<insert id="addWish"
		parameterType="com.tossclone.securities.dto.Wish">
		INSERT INTO wish (user_id, stock_code, stock_name,
		stock_rank, stock_price, stock_volume, stock_rate)
		VALUES (#{userId},
		#{stock.code},
		#{stock.name},
		#{stock.rank},
		#{stock.price},
		#{stock.volume},
		#{stock.rate})
	</insert>

	<delete id="deleteWish" parameterType="com.tossclone.securities.dto.Wish">
		DELETE FROM wish
		WHERE user_id = #{userId} AND stock_code = #{stockCode}
	</delete>
</mapper>
